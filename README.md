# Activity Tracker (MVP 0.1)

Простое и минималистичное desktop-приложение для отслеживания ежедневных активностей. Создано с упором на локальное хранение данных, простоту использования и отсутствие необходимости в интернет-подключении.

Приложение написано на Python с использованием `pywebview` для отображения веб-интерфейса.

<img width="1108" height="934" alt="image" src="https://github.com/user-attachments/assets/965dbfa1-3e0f-45d7-84dd-514c69e5b419" />


## 🌟 Основные возможности

*   **Создание и управление активностями:**
    *   Добавляйте неограниченное количество активностей для отслеживания.
    *   Каждой активности можно присвоить свой тип значения:
        *   ✅ **Отметка** (просто факт выполнения: "Заправил кровать").
        *   🔢 **Количество** (для подсчета повторений: "Подтянулся 10 раз").
        *   ⏱️ **Время** (для отслеживания длительности: "Медитировал 15 минут").
        *   📝 **Текст** (для коротких заметок).
*   **Визуальный календарь:**
    *   Удобная навигация по месяцам и годам.
    *   Дни с выполненными активностями подсвечиваются зеленым.
    *   Пропущенные прошедшие дни отмечаются красным для наглядности.
*   **Гибкая фильтрация:**
    *   Просматривайте прогресс по всем активностям сразу.
    *   Фильтруйте календарь по одной или нескольким активностям (логика "И" - день подсветится, только если выполнены *все* выбранные активности).
*   **Локальное хранение данных:**
    *   Все ваши данные надежно хранятся в одном файле базы данных SQLite на вашем компьютере (`%APPDATA%/MrHouston/ActivityTracker`).
    *   Никакие данные не отправляются в интернет.
*   **Экспорт данных:**
    *   Возможность экспортировать всю историю активностей в JSON-файл через нативное диалоговое окно сохранения.
*   **Продуманный интерфейс:**
    *   Постоянная темная тема для комфортного использования в любое время суток.
    *   Минималистичный дизайн, не отвлекающий от главного.

## 🛠️ Стек технологий

*   **Бэкенд:** Python 3
*   **Веб-фреймворк:** Flask
*   **GUI:** PyWebView
*   **База данных:** SQLite 3
*   **Сборка:** PyInstaller

## 🚀 Установка и запуск (для пользователей)

1.  Перейдите на страницу [**Releases**](https://github.com/MrHouston/Activity-Tracker/releases) вашего репозитория.
2.  Скачайте последнюю версию.
3.  Скиньте в любое удобное место.
4.  Запустите файл `ActivityTracker.exe`.

Все ваши данные будут автоматически сохраняться в папке `%APPDATA%/MrHouston/ActivityTracker`.

## 👨‍💻 Установка и запуск (для разработчиков)

Если вы хотите запустить проект из исходного кода:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/YourUsername/Activity-Tracker.git
    cd Activity-Tracker
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Для Windows
    python -m venv venv
    venv\Scripts\activate

    # Для macOS/Linux
    python3 -m venv venv
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    (Сначала создайте файл `requirements.txt`, если его нет: `pip freeze > requirements.txt`)
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустите приложение:**
    ```bash
    python app.py
    ```

## 📦 Сборка в .exe

Для сборки проекта в исполняемый файл используется `PyInstaller`.

1.  **Установите PyInstaller:**
    ```bash
    pip install pyinstaller
    ```

2.  **Выполните команду сборки из корневой папки проекта:**
    ```bash
    pyinstaller --name ActivityTracker --windowed --add-data "static;static" --add-data "templates;templates" --icon="iconicon.ico" app.py
    ```

3.  Готовое приложение будет находиться в папке `dist/ActivityTracker`.

## 📂 Структура проекта

```
/ActivityTracker/
|
|-- /static/                # CSS и JS файлы
|   |-- /css/style.css
|   |-- /js/
|       |-- activities.js
|       |-- calendar.js
|       |-- export.js
|
|-- /templates/             # HTML шаблоны
|   |-- base.html           # Базовый шаблон с навигацией
|   |-- index.html          # Страница календаря
|   |-- activities.html
|   |-- export.html
|
|-- app.py                  # Основной файл: логика Flask, pywebview API
|-- database.py             # Модуль для всех операций с базой данных
|-- iconicon.ico            # Иконка приложения
|-- README.md               # Этот файл
|-- requirements.txt        # Список зависимостей
```

## 📄 Лицензия

Проект распространяется под лицензией MIT. Подробности см. в файле `LICENSE`.
